import React from 'react';
import './Blog.css'

const Blog = () => {
    return (
        <div>
            <h1 className=' blog-1 text-center rounded-3 w-25 m-auto mb-4 p-2'>Blog</h1>
            <div className='blog-color fw-semibold text-white rounded-3'>
                <h2>what is cors?</h2>
                <p>Cross-Origin Resource Sharing (CORS) is an HTTP-based security mechanism controlled and enforced by the client (web browser). It allows a service (API) to indicate any origin other than its own from which the client can request resources.</p>
            </div>
            <div className='blog-color fw-semibold text-white rounded-3'>
                <h2>what is cors?</h2>
                <p>Cross-Origin Resource Sharing (CORS) is an HTTP-based security mechanism controlled and enforced by the client (web browser). It allows a service (API) to indicate any origin other than its own from which the client can request resources.</p>
            </div>
            <div className='blog-color fw-semibold text-white rounded-3'>
                <h2>what is cors?</h2>
                <p>Cross-Origin Resource Sharing (CORS) is an HTTP-based security mechanism controlled and enforced by the client (web browser). It allows a service (API) to indicate any origin other than its own from which the client can request resources.</p>
            </div>
            <div className='blog-color fw-semibold text-white rounded-3'>
                <h2>what is cors?</h2>
                <p>Cross-Origin Resource Sharing (CORS) is an HTTP-based security mechanism controlled and enforced by the client (web browser). It allows a service (API) to indicate any origin other than its own from which the client can request resources.</p>
            </div>
            <div className='blog-color fw-semibold text-white rounded-3'>
                <h2>what is cors?</h2>
                <p>Cross-Origin Resource Sharing (CORS) is an HTTP-based security mechanism controlled and enforced by the client (web browser). It allows a service (API) to indicate any origin other than its own from which the client can request resources.</p>
            </div>
        </div>
    );
};

export default Blog;